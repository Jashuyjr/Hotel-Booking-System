<?xml version="1.0" encoding="UTF-8"?>
<!--
    web.xml — Deployment Descriptor
    ============================================================
    SYLLABUS TOPIC: Java Web Application Configuration
    
    The web.xml (Deployment Descriptor) tells the Servlet Container
    (Apache Tomcat) how to configure and deploy this web application.
    
    It defines:
      - Welcome file (redirects / to /rooms servlet)
      - Error pages for common HTTP errors
      - Session configuration
      - Character encoding filter
    
    Note: @WebServlet annotations in the Servlet classes replace the
    need for <servlet> and <servlet-mapping> tags in modern Java EE.
    ============================================================
-->
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Hotel Booking System</display-name>
    <description>Grand Vista Hotel Booking System — JSP/Servlet/AngularJS</description>

    <!-- ── Welcome File: root URL redirects to RoomSearchServlet ── -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- Root redirect: /index.jsp → /rooms servlet -->
    <servlet>
        <servlet-name>RootRedirect</servlet-name>
        <servlet-class>com.hotel.servlet.RoomSearchServlet</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>RootRedirect</servlet-name>
        <url-pattern>/index.jsp</url-pattern>
    </servlet-mapping>

    <!-- ── Session Configuration ── -->
    <session-config>
        <!-- Session timeout: 30 minutes of inactivity -->
        <session-timeout>30</session-timeout>
        <cookie-config>
            <http-only>true</http-only>
        </cookie-config>
    </session-config>

    <!-- ── Error Pages ── -->
    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/views/error.jsp</location>
    </error-page>
    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/views/error.jsp</location>
    </error-page>

</web-app>
